name: ManyAgents
options:
  deploymentTarget:
    macOS: "14.0"
  developmentLanguage: en
  createIntermediateGroups: true

configFiles:
  Debug: Local.xcconfig
  Release: Local.xcconfig

settings:
  base:
    SWIFT_VERSION: "5.10"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    # Info.plist reads CFBundleShortVersionString from this, so a plain
    # local build is labelled too. Scripts/release.sh overrides it with
    # the version being released — before this, the flag it passed went
    # nowhere and the bundle kept whatever was hardcoded in the plist.
    MARKETING_VERSION: "0.13.10"

packages:
  Sparkle:
    url: https://github.com/sparkle-project/Sparkle
    from: "2.6.0"

targets:
  ManyAgents:
    type: application
    platform: macOS
    deploymentTarget: "14.0"
    dependencies:
      - package: Sparkle
    entitlements:
      path: Resources/ManyAgents.entitlements
      properties:
        com.apple.security.app-sandbox: false
        # Required under the hardened runtime for microphone capture —
        # without it AVCaptureDevice reports denied even when System
        # Settings grants the app microphone access.
        com.apple.security.device.audio-input: true
    sources:
      - path: Sources/ManyAgents
      # Including Resources/ in sources is how xcodegen wires the asset
      # catalog + fonts into Xcode's resource-compile pipeline. With them
      # only in a separate `resources:` block they get silently dropped
      # and the built bundle has no Resources/ directory at all.
      - path: Resources
        excludes:
          - "Info.plist"
          - "ManyAgents.entitlements"
    settings:
      base:
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: Resources/Info.plist
        CODE_SIGN_ENTITLEMENTS: Resources/ManyAgents.entitlements
        ENABLE_USER_SCRIPT_SANDBOXING: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        COMBINE_HIDPI_IMAGES: YES
